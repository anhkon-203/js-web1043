function chon(x) {
    c = x;
  
    // var arr = ['Rock', 'Paper', 'Scissors'];
    // c = x;
    s = Math.floor(Math.random() * 3);
    chuyensothanhchu(s);
    function chuyensothanhchu(number) {
        var arr = ['Rock', 'Paper', 'Scissors'];
        return bot = arr[number]
    }
    }
